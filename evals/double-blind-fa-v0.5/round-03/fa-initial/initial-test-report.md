# Round 3 FA 证据完整性盲测初报

## 结论

8/8 文件已通过真实本地 HTTP（8891、含 Origin/Referer）上传到默认 `company_intake`，案例为 `case-20260804-405f4af91c`。系统守住了候选证据、人工复核、不得自动生成 Deal/估值/财务计算/评级的边界；但本包仍不可作为已证实事实使用。

本轮复用了安全/证据边界 specialist；其未参与 Round 3 数据或答案生成。只读取了允许的 Round 3 提交、必要运行代码与技能说明，没有读取隐藏真值、其他轮次或工程/Git 消息。

## 产品原生检测分数

**4.0/10（产品控制检测测试分，不是企业、投资、信用、估值或综合风险评级）。** 10 项固定检查按通过 1、部分 0.5、失败 0 计：3 项通过、2 项部分、5 项失败。

- 通过：完整接收并计算上传哈希；保留候选/人工复核边界；未生成 Deal、估值、评级或财务计算。
- 部分：提出来源优先级与现金限制问题，但没有识别具体版本权威，也没有抽取管理层所称现金 4.4。
- 失败：未比对清单哈希；未识别权威/版本/生效/批准/签字；未识别重复；未识别跨表矛盾；通用 `as_of_date` 使合同、公司与来源索引也错误触发财务预检。

## 关键事实

1. `07-artifact-provenance-and-duplicates.csv` 实际 SHA-256 为 `...d0b3`，清单声明为 `...d0b4`；其来源与重复结论在重交前降级。
2. P2601 管理层称已最终验收，但对手方记录显示整改未闭环、最终验收证书不存在、110 万元尾款仍被扣留。
3. P2602 还需单独 PO；P2603 仅为有条件、非约束邮件及未签草案，不能证明“已签约 backlog/确认订单”。
4. 预测 v1.1 较新但未签未批；v1.0 较旧但已签、批准并生效。不得按日期自动选新版。
5. 现金 4.4 没有账户、主体、评估日及受限/质押证明。
6. 公司台账 v1.2 陈旧且未批；v2.0 改变 CEO/ESOP 比例，并披露 IP 质押、联合申请人与东南亚仅有非约束 MOU。
7. P2602 两个文件名共享同一底层哈希，只能算一份证据；且底层文件未随包提交。

## 原生检测与 FA 审查差异

原生系统将 8 个文件全部标为 `generic_supporting`，没有建立权威优先级或矛盾链；预测被报为 `not_present`。同时，7 个 CSV 都因通用日期字段被认作财务元数据，形成错误的预检适用触发。系统提出的 R02-Q06、R02-Q08 有方向价值，但不能替代逐文件的批准、签字、生效、版本、重复与反证核验。

## 精确补件与局部重算

共 8 个问题，逐项证据、影响与局部重算范围见 `question_register.jsonl`。优先顺序是：先修复 07 号索引与底层文件完整性，再核实 P2601、P2602/P2603、预测权威与现金；随后核实股权、IP 与东南亚许可。只有材料被人工接受后，才局部重算受影响的主张、证据、去重、预测或现金输入，不进行跨范围自动升级。

## 数据与遥测标签

企业、交易对手与业务材料均为 **Synthetic**。HTTP 状态、案例 ID、系统计算的上传哈希、识别结果与本地服务状态属于 **真实本地产品遥测**，不是企业事实。
