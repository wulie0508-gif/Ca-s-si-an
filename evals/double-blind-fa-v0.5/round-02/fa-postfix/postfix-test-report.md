# Round 2 FA Post-fix Replay

## Result

**PASS — 100/100.** The product-native `company_intake_preflight` failed closed in both fresh cases. It detected the structured finance pack, reproduced stable `R02-Q01`–`R02-Q08`, blocked calculation, selected no candidate, and kept every supplemental response as unverified candidate evidence.

This was a reused specialist thread because the global thread limit prevented a new thread. It is not presented as brand-new context. The replay was re-executed from the permitted Round 2 inputs and current runtime behavior; prior conclusions were not used as evidence.

## Data classification

- Company names, amounts, manifests, response registers and reconciliation rows: **Synthetic business data**.
- HTTP responses, case IDs, timestamps, artifact counts, port state and server observations: **Real test telemetry** generated by this run.

## Execution

| Item | Observed |
|---|---|
| Transport | Real loopback HTTP on `127.0.0.1:8883` |
| Mutation headers | Same-origin `Origin` and `Referer` supplied |
| Workflow field | Omitted; server defaulted both cases to `company_intake` |
| Initial case | `case-20260804-00814b4a0b`, 6 artifacts |
| Combined case | `case-20260804-9a6d5fdd05`, 9 artifacts |
| Server cleanup | Port 8883 no longer listening; requested PID 8616 absent |

## Gate results

| Gate | Initial 6 files | Combined 9 files |
|---|---|---|
| Applicability | `applicable` | `applicable` |
| Questions | `R02-Q01`–`R02-Q08`, stable order | Same eight IDs, stable order |
| Calculation | `blocked`; not performed | `blocked`; not performed |
| Selected basis | All `selected_candidate` fields `null` | All `selected_candidate` fields `null` |
| Forecast selection | `selected_version=null` | `selected_version=null` |
| Source precedence | `selected_precedence=null` | `selected_precedence=null` |
| Candidate receipts | 0 | 8 |
| Truth upgrade | None | Every `accepted_as_truth=false` |
| Question closure | All open | Every `question_closed=false`; all questions open |
| Next action | `review_financial_basis_questions` | `review_financial_basis_questions` |

The combined submission therefore did **not** treat a declared `full` response for `R02-Q04` as verified truth and did not close the question. The product also did not choose a forecast because it was newer, choose a cash value because it was larger, or reconcile similarly named fields by guesswork.

## Deal, valuation and calculation boundary

- `/api/ui/deals` returned zero deals in the fresh workspace.
- `deal_or_valuation_created=false` for both cases.
- `financial_calculation_performed=false` for both cases.
- `aggregate_rating_produced=false` for both cases.
- No Deal, valuation or calculation mutation endpoint was invoked.

## UI smoke

The live server returned HTTP 200 for `/`, `/agent_bridge.css` and `/agent_bridge.js` with non-empty payloads. A headless DOM render was attempted but the local Chrome invocation returned no dump, so a browser-render assertion is deliberately not claimed. This optional limitation does not alter the deterministic gate result and can be covered by the parent browser QA.

## Verdict

The Round 2 post-fix behavior satisfies the requested FA gate: enterprise-supplied normalization and precedence declarations are recorded as candidate responses, not promoted to facts; critical basis questions continue to control the next action; calculation and downstream Deal/valuation paths remain closed.
